
spring:
  datasource:
    url: jdbc:h2:mem:test;DB_CLOSE_ON_EXIT=TRUE
    driverClassName: org.h2.Driver
    username: test
    password: test
  flyway:
    enabled: true
    locations: classpath:/db/migration/structure,classpath:/db/migration/test-data
    validate-on-migrate: true
  h2:
    console:
      enabled: false


